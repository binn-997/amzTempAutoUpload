# ============================================================
#  Amazon 商品数据处理工具 — Python 依赖清单
# ============================================================
# 安装方式: pip install -r requirements.txt
# 最低 Python 版本: 3.10

# ── 核心依赖（必须）──────────────────────────────────────────
# Excel 读写与数据处理
pandas>=2.0.0,<4.0.0
openpyxl>=3.1.0,<4.0.0

# ── 配置文件解析（可选）──────────────────────────────────────
# YAML 配置文件支持；如仅使用 JSON 格式配置，可跳过此依赖
pyyaml>=6.0,<7.0

# ── 构建依赖（仅打包 .exe 时需要）────────────────────────────
# 日常使用无需安装，仅在打包发布时使用
pyinstaller>=6.0,<7.0
pyinstaller-hooks-contrib>=2024.0

# ── 开发依赖（可选，建议开发者安装）──────────────────────────
# pip install -r requirements-dev.txt
# 类型检查: mypy
# 代码格式化: ruff
# 测试框架: pytest
